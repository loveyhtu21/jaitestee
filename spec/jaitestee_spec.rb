require 'spec_helper'

describe Jaitestee do
  it 'has a version number' do
    expect(Jaitestee::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
